# Артефакты браузерных агентов
.playwright-mcp/
*.log

# Зависимости
node_modules/

# Секреты и сертификаты
.env
certs/*.pem
certs/*.cer

# Скачанные данные (перечень 8 МБ, адресный реестр — большие, качаются заново)
data/*.pdf
data/*.json

# Логи прогонов
logs/
