# КОСМОС — хэндофф

**Обновлён:** 2026-07-28
**Репозиторий:** https://github.com/VseSamo-git/milti (приватный, основной).
Старый remote `origin` → `kosmos-mealty` сохранён и синхронизируется; новый remote `milti` — основной.
**Свежее — см. раздел 10 (обновление 2026-07-28):** репозиторий, задачи 2–3, сверки, фундамент.

Документ для того, кто продолжает работу с нуля. Читать целиком один раз.

---

## 1. Что это и для кого

**Заказчик:** Дима, директор по развитию ООО «МИЛТИ» (`mealty.ru`, ИНН 7722413867). Готовые блюда, ~500 торговых точек в Москве и СПб. Клиент — офисный сотрудник с коротким обедом, поэтому точка должна быть не дальше минуты ходьбы от работы.

**КОСМОС** — база лидов отдела развития: реестр коммерческих зданий Москвы, чтобы находить, куда открывать новые точки. Это первый слой; «планеты» 1–7 (маршруты менеджеров, Avito/Cian, CRM, юристы, стройка, фото, HR) — потом, отдельными проектами.

**Что просит Дима:** семь листов (БЦ, офисные здания, офисы компаний, НИИ, ВУЗы, ТЦ с супермаркетом, конкуренты), вычитание своих и закрытых точек, обновление каждое воскресенье в 10:00, плюс гипотезы и новости рынка.

**Главные документы:**
- Спека: `docs/superpowers/specs/2026-07-15-kosmos-design.md` (ред. 3)
- План: `docs/superpowers/plans/2026-07-16-kosmos-registry-baseline.md`
- Для Димы: `docs/для-Димы/РЕЗЮМЕ.md` — человеческим языком, без терминов

---

## 2. Как это работает

**Три источника, у каждого своя роль:**

| Источник | Отвечает на вопрос | Стоимость |
|---|---|---|
| Перечень 700-ПП (PDF с mos.ru) | какие здания Москвы коммерческие | 0 ₽ |
| Росреестр (геопортал НСПД) | сколько в здании метров | 0 ₽ |
| OpenStreetMap (Overpass) | ВУЗы, НИИ, супермаркеты | 0 ₽ |
| Store-locator'ы сетей | конкуренты (сайт каждой сети) | 0 ₽ |
| 2ГИС | **только** маркетинговое название БЦ | нет ключа |

**Порядок сборки — в нём вся суть экономики:**

```
42 650  перечень 700-ПП        ← город уже решил «это коммерция»
   ↓     только приложение 1 (здания)
22 863  зданий
   ↓     Росреестр: площадь по каждому (~6,4 ч при 1 req/s)
   ↓     ФИЛЬТР 10 000 м²      ← здесь воронка схлопывается
 ~3 400  целей (оценка)
   ↓     обогащение только для них
```

Дорогое — последним, по немногим. Обратный порядок сделал бы проект неподъёмным.

---

## 3. Состояние на 2026-07-16

### База (Supabase, PostgreSQL 17.6)

| Что | Сколько |
|---|---|
| `objects` | 42 650 (из них зданий 22 863) |
| обогащено площадью | **692 (3%) — идёт** |
| от 10 000 м² | 16 (крупнейшее 72 874 м²) |
| с UNOM | 21 044 (**92% по зданиям**) |
| с координатами | **20 922 (91% по зданиям), цели 122/122 — новое 2026-07-20** |
| `observations` | 694 (сырые ответы Росреестра) |
| `places` | НИИ 472, ВУЗы 364, колледжи 286 |
| **конкуренты** | **635 — 8 сетей со store-locator'ов сетей (2026-07-20)** |

Конкуренты по сетям (со store-locator'ов, только Москва): french bakery 153, правда кофе 151, шоколадница 123, братья караваевы 78, prime 76, bodro 28, муму 14, дни недели 12.

**ВАЖНО — эти числа заменили ошибочные OSM-цифры.** Прежний сбор из OpenStreetMap
давал 543 точки и был выдан за «все 11 сетей, обход полный». Это оказалось
неправдой: OSM — краудсорсинг, покрытие 4–141% и непредсказуемо. French Bakery
там было 6 из 154 реальных (4%), Правда кофе 81 из 153 (53%), Prime 101 при ~76
(регэксп ловил «АЭИ ПРАЙМ», такси, стоматологии). Теперь каждая сеть берётся с
её собственного сайта, а канарейка (см. `src/sources/competitors.js`) сверяет
разбор с числом, которое сеть пишет о себе, и не даёт молча записать недобор.

**3 сети без первоисточника** (drinkit, здрасте, parle market): их сайты за
анти-бот защитой или без своего списка адресов. Обход защиты НЕ встраивали —
они честно помечены «нужен вторичный источник», а не показаны нулём. По ним
нужен отдельный сбор (карты/агрегаторы, low confidence).

### Готово и проверено на живых данных

- Парсер 700-ПП: 42 650 номеров за 0,3 сек
- Клиент Росреестра: площадь, этажи, год, адрес. 92% покрытие
- Карта UNOM: 285 782 пары (выгрузка 45 мин)
- Коллекторы OSM: конкуренты, ВУЗы, НИИ, супермаркеты
- Фильтры, серая зона, вычитание по UNOM
- Координаты зданий из адресного реестра (60562, `fetchGeodata`): 91% по зданиям
- 74 теста, все зелёные

### Не сделано (на 2026-07-16; актуализировано в разделе 10)

- Воскресный конвейер в n8n
- Telegram-бот и дайджест
- LLM-аналитик (задача 4 Димы: гипотезы, партнёры, брокеры, новости)
- Связка «координата → здание» (координаты уже есть — 91% зданий; сам джойн точка→здание для площади ТЦ/ВУЗов ещё не написан, но разблокирован)
- Ветка приложения 2 (19 787 помещений в зданиях без кадастра)
- NocoDB не развёрнут

> С 2026-07-28 активированы **задачи 2 (маршруты) и 3 (Avito/Cian)** — построен их общий
> фундамент (движок одобрений + справочник метро). Подробности и новый список «что дальше» — **раздел 10**.

---

## 4. Блокеры

| Что нужно | От кого | Без этого |
|---|---|---|
| **Списки точек МИЛТИ** (действующих и закрытых) | Дима | вычитание не работает — код готов, вычитать нечего |
| **SSH к makersai.ru** | пользователь | NocoDB не поднять |
| **Ключ 2ГИС** (письмо на dev@2gis.ru) | — | не будет названий БЦ. **Не блокер**, всё остальное закрыто |
| **Юрист МИЛТИ** | Дима | три вопроса из спеки, раздел 9 |

**Про NocoDB — важно:** облачный бесплатный тариф **не умеет** подключать внешний Postgres (только Business+). Self-hosted через докер — умеет, бесплатно. Значит нужен SSH.

**Пока NocoDB нет, Дима смотрит базу в Supabase Table Editor** — сетка с фильтрами, работает уже сейчас. `supabase.com` → проект «Милти» → Table Editor → схема `kosmos`.

---

## 5. Как запустить

```bash
npm install
node run.js ./scripts/apply_schema.js            # схема (идемпотентно)
node run.js ./scripts/build_baseline.js parse    # перечень → реестр
node run.js ./scripts/build_baseline.js enrich   # площади, ~6,4 ч, ВОЗОБНОВЛЯЕМО
node run.js ./scripts/build_baseline.js unom     # UNOM для вычитания
node run.js ./scripts/build_baseline.js subtract # вычесть точки Димы
node run.js ./scripts/build_places.js all        # OSM: конкуренты, ВУЗы, НИИ
npm test
```

**`run.js` обязателен** — он подгружает `.env` и сдвигает аргументы. Без него скрипт не увидит стадию.

**Обогащение возобновляемое:** упало — просто запустите заново, продолжит с места обрыва (`pendingCadastrals` берёт только необогащённые). Часы работы не теряются.

**Доступы:** `.env` (в `.gitignore`, в репозиторий не попадает). Сертификат — `certs/ru-bundle.pem`, тоже игнорируется. Пересобрать:
```bash
curl -o certs/root.pem https://gu-st.ru/content/lending/russian_trusted_root_ca_pem.crt
curl -o certs/sub.pem  https://gu-st.ru/content/lending/russian_trusted_sub_ca_pem.crt
# ВАЖНО: между сертификатами нужен перевод строки, иначе Node даёт «bad end line»
awk 'FNR==1 && NR>1 {print ""} {print}' certs/root.pem certs/sub.pem | sed '/^$/d' > certs/ru-bundle.pem
```

---

## 6. Грабли, оплаченные временем

Не наступайте заново.

**PDF перечня.** Subset-шрифты: кириллица в глифах, кернинг режет кадастровые номера на куски (`50:09` + `:0000` + `000:1`). Наивная регулярка возвращает **ноль молча, без ошибки**. Надо склеивать литералы внутри блоков TJ. Адреса из PDF не доставать — берите `readable_address` из Росреестра, он чище.

**Supabase без TLS** отвергает соединение, но врёт про причину: отдаёт `28P01 password authentication failed`. Пароль ни при чём. Нужен `ssl: 'require'`.

**`NODE_EXTRA_CA_CERTS`** читается Node только при **старте процесса**. Выставить из кода нельзя — все запросы молча падают в `fetch failed`. Клиент НСПД подключает сертификат своим диспетчером undici.

**`Agent` из npm-пакета `undici`** не принимается **встроенным** в Node `fetch` (`UND_ERR_INVALID_ARG`) — это разные экземпляры библиотеки. Берите `fetch` из того же пакета.

**Referer для НСПД обязателен**, иначе 403. Причём `nspd.gov.ru/map` и `nspd.gov.ru/` заблокированы **специально** — ими подписываются наивные скраперы. Работает `https://nspd.gov.ru/map?thematic=PKK`.

**Overpass:** без User-Agent — 406. Запрос через `area["name"="Москва"]` + регэксп по многим сетям — 504. Нужен bbox и по одной сети за запрос, с ретраями. (Для ВУЗов/НИИ/супермаркетов актуально; конкуренты переехали на store-locator'ы.)

**Конкуренты: OSM — ложное дно.** Собирать конкурентов из OSM регэкспом по имени НЕЛЬЗЯ — проверено 2026-07-20. Покрытие 4–141% и непредсказуемо: French Bakery 6 из 154 (краудсорсинг её почти не нанёс), Prime 101 при ~76 (регэксп `/Prime|Прайм/` ловил «АЭИ ПРАЙМ», «Дента прайм», такси, стоматологии). Фильтр `re.test(el.name)` тут бесполезен: он гоняет ТОТ ЖЕ регэксп, что Overpass уже применил на сервере, — отсеять лишнее не может по построению. Решение: адаптер на сеть в `src/sources/competitors/`, читает сайт самой сети. Канарейка (`checkCoverage`) сверяет разбор с числом, которое сеть пишет о себе, — недобор уходит в `failed`, а не в базу. Три сети (drinkit, здрасте, parle) за анти-бот защитой / без своего списка: обход НЕ встраивали, помечены «нужен вторичный источник».

**Координаты data.mos.ru молча теряются без `fetchGeodata:true`.** Датасет 60562 (адресный реестр) отдаёт `geodata_center` ТОЛЬКО когда в теле POST стоит `fetchGeodata:true`. Без флага — те же строки без гео, без ошибки: 0 из 200 с координатами, с флагом — 200 из 200. Мы полгода качали реестр и выбрасывали координаты. Порядок осей GeoJSON `[lon, lat]`: широта Москвы ~55, долгота ~37 — перепутать значит отправить всё в Индийский океан. На это есть тест-страж в `addr_registry.test.js`. Проверено 2026-07-20.

**Кириллица в аргументах curl на Windows (Git Bash) приходит битой.** Запрос к Overpass или порталу с русским текстом в параметре молча возвращает 0 объектов — не ошибка, а искажённая кодировка на границе шелла. Прошлые замеры OSM могли быть занижены по этой причине. Лечится записью запроса в UTF-8 файл и `--data-urlencode data@файл`. Та же природа, что у бага PDF из этого раздела: **ноль молча**.

**HTTP 5xx от портала — временная ошибка, но `isTransient` её раньше не ловил.** Под `fetchGeodata` запрос тяжелее и портал изредка отдаёт 504. `withRetry` лечит, но только если `isTransient` признаёт статус временным — теперь признаёт 429/500/502/503/504 (см. `retry.js`). 4xx (кроме 429) остаются постоянными: наша ошибка запроса повтором не лечится.

**Порог покрытия UNOM** считайте только по зданиям (`annex=1`). По всем объектам выйдет 49% (у помещений номер комнаты, которого в карте зданий нет) — и сборка ложно оборвётся. По зданиям — 92%.

---

## 7. Решения, которые выглядят странно, но обоснованы

**Почему не Яндекс.Карты.** Не отдают ни площадь, ни этажность — ни за какие деньги. И лицензия дословно: «Расширенной лицензии для этого сервиса нет, так как **запрещается сохранять или изменять данные**». Расширенной лицензии для Geosearch **не существует** — запрет не снять деньгами.

**Почему не ads-api / cianparser / парсеры.** Они видят **объявления**, а не реестр. БЦ, забитый арендаторами, объявлений не даёт и невидим — а это лучшая цель. Проверено: `cianparser` (195★) парсит только жильё, код с февраля 2024. Сервисы для планеты 2, не для КОСМОСа.

**Почему не ЕГРЮЛ.** 150 000 ₽/год при бюджете 120 000. Юрадрес ≠ фактический офис. ФНС считает адрес массовым от 5 организаций и держит это как **признак недостоверности** — прокси антикоррелирует с целью.

**Почему вычитание по UNOM, а не по радиусу.** Первая редакция вычитала в радиусе 150 м — ошибка. «1 минута ходьбы» — это критерий близости к клиенту, а не каннибализации. Радиус исключения (150 м) был **больше радиуса обслуживания** (80–100 м): правило вычитало здания, которые точка обслужить не может. Одна точка в Башне Федерация стирала Меркурий и Империю — 400 тыс. м². И чем больше точек у МИЛТИ, тем меньше лидов видела система. Сейчас — точное совпадение UNOM, а расстояние стало просто колонкой.

**Почему трафик не считаем.** Норматив рабочего места 10–12 м² на человека → 10 000 м² офисов и есть 700–1000 сотрудников. Дима задал **один критерий дважды**, в разных единицах. Площадь у нас официальная.

**Почему нет Google Sheets.** Дима просил именно их, но потому что не знал про NocoDB. Sheets — это копия, а копия порождает костыли (беречь правки от перезаписи, вердикты писать некуда). Покажем NocoDB и спросим. Скажет «нужно» — час работы.

**Почему `objects` и `places` раздельно.** Шоколадница — точка **внутри** здания, у неё нет кадастрового номера; ВУЗ может занимать несколько корпусов. Класть их в реестр зданий значит сломать ключ и правило «один объект = одно здание».

---

## 8. Принципы, которые нельзя нарушать

**Провенанс.** Ни одно значение без источника. **NULL легитимен** — пустая ячейка честнее выдуманной. Это не дисциплина, а механика: CHECK-констрейнты не дадут записать площадь без `area_source`. Проверено на живой базе.

**Машина пишет в `objects`, человек — в `verdicts`.** Никогда наоборот. В NocoDB вид на `objects` обязан быть read-only, иначе прогон обогащения затрёт правку Димы.

**Система никогда не удаляет.** Только меняет статус. Пропавшее из выдачи — «не найден в этот раз», а не стёрто. Иначе один сбой источника выглядит как «в Москве закрылось 400 БЦ».

**Частичный сбой не молчит.** Если сеть не ответила — её точки **нельзя** метить закрытыми: Дима прочтёт, что Шоколадница ушла из Москвы. При неполном обходе пометка пропускается с предупреждением.

**Правовые инварианты по Росреестру** (спека, раздел 9; штраф юрлицу 350–600 тыс. ₽):
1. Хранить **только производные поля** (площадь, этажи, год) в своей табличной форме. Никогда не воспроизводить форму выписки.
2. **Никогда** не хранить и не пересылать файлы с ЭЦП Росреестра — ни в базу, ни в Telegram.

Это условия изъятия ч. 27 ст. 62 ФЗ-218 — единственная законная опора. Обоснование «мы внутри и не перепродаём» **не работает**: ч. 25 запрещает создание программ доступа к ЕГРН без слов «за плату».

---

## 9. Что делать дальше

**Сразу, ни от кого не зависит:**
1. Дождаться обогащения (~6 ч) → `subtract` → показать реальные цифры по БЦ.
2. Связка «координата → здание»: даст площадь для ВУЗов, НИИ и отделит ТЦ от Пятёрочки на первом этаже жилого дома.
3. Воскресный конвейер в n8n (**не трогать существующие воркфлоу** — при необходимости копировать в проект).
4. Telegram-бот + дайджест.
5. LLM-аналитик — задача 4 Димы.

**Когда придут списки точек Димы:**
Прогнать `subtract`. **Обязательно** сначала регресс-тест: если одна существующая точка МИЛТИ исключает другую — правило неверно по построению, сборку не запускать.

**Открытый вопрос без ответа:** отличить БЦ от торгового центра внутри перечня нечем. Город метит и то и другое как «коммерцию», Росреестр — как «нежилое». `building_name` из ЕГРН проверен и **не годится**: в Москве осмысленных 24%, и те мусор («учреждение», «компрессорная»). Частично спасает то, что ТЦ ловим отдельно от супермаркетов. Полностью — только 2ГИС.

---

## 10. Обновление 2026-07-28 — репозиторий, задачи 2–3, сверки, фундамент

### 10.1 Репозиторий переехал

Основной репозиторий теперь **https://github.com/VseSamo-git/milti** (приватный, аккаунт
VseSamo-git). Кириллицу в имени GitHub не принимает, поэтому `milti` латиницей. Локально
два remote: `milti` (основной, ветка `main` отслеживает его) и `origin` → старый
`kosmos-mealty` (сохранён, синхронизируется). Пушим в оба.

### 10.2 Активированы задачи 2 и 3 (планеты 1–2)

Дима прислал ТЗ на две задачи поверх готового КОСМОСа:
- **Задача 2 — маршрутизация.** Ежедневно до 9:00 бот собирает маршрут каждого менеджера
  из листов базы и шлёт в Telegram (одно сообщение = один менеджер): объект — адрес — лист —
  станции метро. Дима отвечает «ок» или голосом с правками (транскрибируем), пока не одобрит —
  напоминаем.
- **Задача 3 — мониторинг Avito/Cian.** Ежедневно в 9:30 бот шлёт новые объявления по
  сохранённым поискам Димы (нумерованные ссылки). Дима отвечает номерами → ближайшие адреса
  одобренных уходят в маршрут (стык с задачей 2).

**Спека:** `docs/superpowers/specs/2026-07-28-marshrutizatsiya-i-monitoring.md`.
**Вопросы Диме (11 шт., ждут ответа):** `docs/для-Димы/вопросы-маршруты-и-мониторинг.md`.

Решения Эдуарда: источник «БАЗЫ» — NocoDB/Postgres (не Google Sheets); модель менеджеров —
позже/решит Дима; транскрипция — **OpenAI Whisper**; движок одобрений — общий для обеих задач.

### 10.3 Построен фундамент обеих задач (всё через TDD, набор 117 тестов зелёный)

- **Движок одобрений** — `src/lib/approvals.js` (`classifyReply` / `shouldRemind` /
  `matchReply`): чистая логика «отправил → жду вердикт → напоминаю», без БД и Telegram.
  Тесты — `tests/approvals.test.js`.
- **Миграция состояния** — `db/migrations/013_approvals.sql` (таблица `kosmos.approvals`).
  ⚠️ **НЕ применена к боевой Supabase** — ждёт отмашки.
- **Справочник метро** — `src/lib/metro.js` (`parseStations` / `nearestStations`), тесты
  `tests/metro.test.js`. Собрано **225 станций** метро Москвы из OSM →
  `docs/metro.json` (команда `node scripts/fetch_metro.mjs`).

### 10.4 Сверки базы с внешними источниками

**Арендатор (arendator.ru), 782 БЦ ≥10к.** Сайт отдаёт чистый JSON-LD с адресом и
координатами, анти-бота нет. Скрипт `scripts/fetch_arendator.mjs` тянет все 9 страниц →
`docs/arendator_bc.json` (ручные сейвы браузером больше не нужны). Сверка по координатам
(порог 80 м, `scripts/reconcile_arendator.mjs`): **644 из 782 уже в базе**, 138 нет — из них
56 Подмосковье (вне scope, см. вопрос 10 Диме) и 82 Москва (35 в 80–120 м — тот же дом с
оффсетом, 37 пограничных, **10 точно отсутствуют**). Вывод: реестр 700-ПП по Москве почти
полон, реальный пробел — единицы, а не сотни.

**Excel Димы** (`Арендатор/БЦ_новые_2025-2026.xlsx`, лист «БЦ введённые 2025–2026»). 24 БЦ,
из них только 2 в базе (STONE Ленинский, Тверская 24) — **22 новых**: новостройки 2025–2026
класса A/A+ (iCITY 230к, НКЦ 250к, SLAVA, Штаб-квартира Яндекса, СберСити…), которых нет в
статическом 700-ПП. Самые горячие лиды. Скрипт `scripts/reconcile_excel_bc.mjs`, данные —
`docs/reconcile_excel_bc.json`. Ждут ингеста отдельным источником (`area_source='press'`,
кадастра/UNOM нет, координаты геокодить).

### 10.5 Что делать дальше (актуальный список)

**Не ждёт никого:**
1. Применить миграцию 013 + слой доступа к `kosmos.approvals` (read/write).
2. Расширить `scripts/run_bot.js`: исходящие сообщения, привязка reply-to, скачивание голоса `.ogg`.
3. Джойн метро → объекты витрины (nearest станция к каждому БЦ через `metro.js`).
4. Ингест 22 новых БЦ из Excel + ~10 точно отсутствующих БЦ Арендатора (Москва).

**Блокеры:**
- 🔑 **Ключ OpenAI (Whisper)** — без него не поедет транскрипция голосовых правок (текстовые — работают).
- 👤 **Задача 2:** ответы Димы про менеджеров (кто, сколько объектов/день, как делить, дни).
- 🔗 **Задача 3:** ссылки сохранённых поисков Димы + адаптеры Avito/Cian (площадки анти-бот, риск как с конкурентами в §6).
- ❓ **Вопрос 10 Диме:** нужны ли БЦ Подмосковья (56 из сверки Арендатора вне текущего scope).

**Артефакты сверок и справочники** лежат в `docs/`: `arendator_bc.json`, `arendator_missing`
(внутри `reconcile_arendator.json`), `reconcile_excel_bc.json`, `metro.json`.

---

## 11. Обновление 2026-07-29 — сервер, лист 1, вычитание, уход с Supabase

Эта сессия работала по задаче 1 (списки БЦ) и инфраструктуре. Всё закоммичено, оба remote.

### 11.1 Обогащение НСПД переехало на сервер 24/7

Ноут засыпал и рвал прогон. Теперь обогащение крутится на **makersai.ru** (Beget VPS,
`ssh -i ~/.ssh/beget_n8n root@makersai.ru`) в docker-контейнере **`kosmos-enrich`**
(`node:22-alpine`, `--restart unless-stopped`, код в `/root/kosmos`, цикл enrich→sleep 300).
**НСПД отдаёт 200 с дата-центрового IP** (бытовой банился — датацентр нет). Очередь org-first:
здания с арендаторами (`org_count>0`) дают ≥10k в 30% против 8% — их спрашиваем первыми.
Обогащено ~21.5k из ~23k, хвост дотикивает.

### 11.2 Лист 1 «БЦ Москвы» перестроен: реестр вместо OSM-метки

Была хрупкая `object_type='бц'` (совпадение имени OSM в 60 м) — прятала сотни. Теперь лист =
весь реестр 700-ПП ≥10k минус ретейл, с плашкой **«Профиль»**. Подтверждение БЦ двумя источниками:
- **arendator.ru** — сверка по координатам ≤80 м (`scripts/link_arendator.js`, колонки `arendator_*`),
- **OSM-имя** — как раньше.
Итог: **522 подтверждённых БЦ** (было 260). Остаток режется геометрией офиса (площадь/этаж ≤3000
= офис, шире = склад/молл). Миграции `011_bc_srednie.sql` (лист 8, средние 5–10к), `012_bc_reestr.sql`,
`014_arendator.sql`. Колонка «Подтверждён» показывает источник (OSM / Арендатор / оба).

### 11.3 Вычитание точек МИЛТИ (давний блокер снят)

Дима дал списки открытых и закрытых точек доставки (`Открытые и закрытые точки.xlsx`, листы
work/closed). По этим адресам БЦ не ищем. `scripts/link_milti_points.js`: ингест в
`our_points`/`closed_points` + пометка совпавших `status=вычтен_*`. Матч ≤30 м **или** совпадение
нормализованного адреса (Дима: «точнее + по названиям»). **Вычтено 609** (226 работающих + 383
закрытых). NB: исключение вычтенных из витрины придёт со сборкой единой «Базы».
Ре-ингест (Дима обновляет раз в 3 мес) — через `KOSMOS_MILTI_CSV_DIR`.

### 11.4 Phase B — ушли с Supabase на свой Postgres ✅

**Supabase больше нет на критическом пути.** Причина: под нагрузкой демона его пулер стабильно
подвешивал тяжёлые запросы; плюс free-tier усыпляет базу, коммерческая тайна на чужом облаке.
База переехала в контейнер **`kosmos-postgres`** (`postgres:17`, docker-сеть `web`, том
`kosmos_pgdata`) — рядом с NocoDB. Скрипт `/root/kosmos/migrate_pg.sh` (= `infra/migrate_pg.sh`):
дамп схем `kosmos`+`vitrina` из Supabase → restore → сверка строк (42650=42650) → cutover `.env`.
Supabase цел как бэкап, старый `.env` в `/root/kosmos/.env.supabase.bak`.
**NocoDB не трогали** — он push-fed (Postgres→NocoDB по API), витрину не сломало.

**⚠️ Новый воркфлоу:** `kosmos-postgres` виден только внутри сети `web` — с ноута не достучаться.
ВСЕ скрипты с базой запускаем НА СЕРВЕРЕ:
`docker run --rm --network web -v /root/kosmos:/app -w /app node:22-alpine node run.js ./scripts/X.js`.
Код на сервер — `tar czf - src scripts db certs run.js package*.json | ssh … 'tar xzf - -C /root/kosmos'`
(БЕЗ `.env`). Секреты и подключения к Supabase агент двигать не может (классификатор рубит как
эксфильтрацию) — такие шаги (scp `.env`, дамп) запускает пользователь руками.
Упоминания «Supabase» в разделах 3/10 выше — исторические. Миграция `013_approvals` (раздел 10.3)
теперь применяется к ЛОКАЛЬНОЙ базе, не к Supabase.

### 11.5 Ответы Димы по маршрутам (задача 2)

Получены и сохранены в `docs/ответы-Димы-маршруты.md`: **5 менеджеров** («Менеджер 1…5»),
**10 адресов/день**, только будни, деление по **секторам** (Дима пришлёт границы, маршрут из
10 — в рамках одного сектора; можно +10 в том же или 10 в другом). В маршрут — все типы.
Посещённые не исключать, давать другим. Одобрение Димой → история (дата–менеджер–тип), не
одобрен → напоминать через час. **Вопрос 10 закрыт:** БЦ Подмосковья нужны, критерий тот же >10k.

### 11.6 Что делать дальше

**Согласована структура единой базы** (цель Димы): один лист **«База»** (все подтверждённые:
БЦ/офисные/ВУЗ/НИИ/ТЦ-с-продуктовым/конкуренты, столбец «Тип объекта», вычтенные убраны) +
лист **«На проверку»** (столбец «Решение» ОК/Хуй: ОК→в Базу, Хуй→удалить, через вердикты) +
листы «Открытые точки»/«Закрытые точки». Дима — владелец, управляет через **бота** (человеческий
слой, обогащение не затирает), в NocoDB редактирует назначенные поля.

Очередь: (1) дособрать Арендатора 800+ (`fetch_arendator.mjs` есть) и добавить недостающие;
(2) влить БЦ Макса — **+57 новых** (`база от макса.xlsx`, замерено, не влито; кадастра нет →
внешние объекты); (3) проверить ВУЗ/НИИ/конкуренты на реальность (мусор типа автобусных маршрутов
в НИИ); (4) собрать единую «Базу» + «На проверку»; (5) расширить бота (добавить/изменить/удалить
объект в человеческий слой).

### 11.7 Блокеры — обновлено

- ✅ ~~SSH к makersai~~ — есть (`root@makersai.ru`, ключ `beget_n8n`).
- ✅ ~~Списки точек МИЛТИ~~ — получены, вычтены (609).
- ✅ ~~Supabase~~ — ушли на свой Postgres.
- 🔑 **Ключ OpenAI (Whisper)** — для транскрипции голосовых (задача 2).
- 🔑 **Ключ 2ГИС** — названия БЦ, кол-во сотрудников (не блокер, но полезно).
- 🤖 **Токены бота** — `ANTHROPIC_API_KEY`, `KOSMOS_TELEGRAM_TOKEN`, `KOSMOS_TELEGRAM_ALLOWED` (Telegram-id Димы).
- 🔗 **Задача 3:** ссылки сохранённых поисков Димы (обещал на выходных) + адаптеры Avito/Cian.
